# 04-Journal/Decisions

One note per significant decision: what was decided, why, and what alternatives were
considered. `status: active` once decided; if a later decision reverses this one, set
`status: superseded` and `superseded_by: "[[Successor]]"` on the old note. Never delete
a decision note, an overturned decision is still useful history.
