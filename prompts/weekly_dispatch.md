You are preparing the weekly dispatch package for a personal knowledge vault
that runs an autonomous write gate. Most days flow straight into the canon by
themselves; the items below are the ones that needed a human. Reply in
{{MAIN_LANGUAGE}}, with no preamble, no markdown code fence around the whole
answer, and never use an em-dash (U+2014) or en-dash (U+2013) anywhere (use a
comma, colon, parentheses, period, or conjunction instead).

## Escalated gate queue

{{QUEUE}}

## This week's digest

{{DIGEST}}

## What to produce

Produce exactly these two sections, in {{MAIN_LANGUAGE}}:

### Queue dispatch

A numbered table: `#` | short title | type | proposed destination |
recommendation (approve / edit / discard, plus one short sentence of why).
Base every recommendation strictly on the evidence shown for that candidate;
if you are not confident, recommend "escalate further" rather than guessing.
If the queue is empty, say so in one line and skip the table.

### Digest draft

2 to 3 short topics drawn only from this week's digest above, each one:
**hook** then what was learned or decided then how to apply it (3 to 5 lines
per topic, direct voice). If the digest is empty, say so in one line.
